# 著作規約 (botrail カタログ準拠)

このリポジトリにアセットを追加・変更するときの規約。利用者向けの説明は
[README.md](./README.md) を参照。

## 参照実機を必ず 1 つ定める

自作アセットは**ゼロから想像で作らない**。実在する製品を 1 つ「参照実機」に定め、
**その公表値に寸法・性能を合わせた上での独自著作**にする。README に次の 3 つを必ず書く:

1. **参照実機の型番と出典 URL** — 「〜クラス」「一般的な〜」のような曖昧な書き方はしない。
   型番まで特定できない値は、特定できないと明記する
2. **参照実機から採った値の一覧** — 公表値と本アセットの値を並べた表にする
3. **意図的に変えた点と、その理由** — ただし下記の「実機忠実が優先」に従うこと。
   変えた場合は隠さず差分として書く

アセットのディレクトリ名にも参照実機の型番 (またはシリーズ名) を入れる
(例: `vacuum-gripper-ecbpi`, `tool-changer-sws011`, `weld-gun-x16005`)。

## 実機忠実が優先 — 組み合わせの都合で寸法を描き替えない

利用者がまず試したいのは「**手持ちの実機でシミュレーションできるか**」なので、
**実機忠実がこのリポジトリの核となる方針**。とくに**取付インタフェースは実機どおりにする** —
カタログ内の他モデルと直結させたいからといって ISO 9409-1 等へ描き替えてはいけない。

直結できない組み合わせは、**実在するアダプタ (フランジプレート等) を別アセットとして
作って解決する**。メーカーも実際にロボット別のフランジプレートを別売しているので、
これが最も忠実かつ組み合わせも成立する解になる
(例: flange-plate-ecbpi = Schmalz ROB-SET のフランジプレート相当)。

参照実機の**寸法図そのものは複製しない**。採ってよいのは公表されている数値 (facts) で、
外観は独自著作にする。**再配布を制限している文書 (社外秘条項・複製禁止条項つきの
マニュアル等) は寸法ソースに使わない** — 制限のない公式製品ページ・データシート・
公開規格を選ぶこと。ND 条項つきのベンダ CAD は「測定器」としてのみ使い、テッセレート
したメッシュやその派生物をリポジトリに置かない (例: tool-changer-sws011 の
docs/step-measurements.md)。適切な参照実機が見つからない場合は、探した範囲を README に
記録する (例: tool-changer-sws011 が ATI QC-11 を参照実機にしなかった理由)。

## 形状・データの規約

- Z-up / メートル単位
- リンク・フレーム名は USD-safe (`[A-Za-z_][A-Za-z0-9_]*`)
- ツール類: root リンク `mount` = フランジ接触面 (+Z がツール内側へ向く)。
  `tcp` は固定リンクとして URDF 内で宣言 (recipe の extra_frames 不要)
- **visual と collision を分ける**。visual はメッシュで作り込んでよいが、
  開口を持つツール (ガン等) の collision は **URDF プリミティブ (box/cylinder)
  のみ**で著作して開口を保つ (カタログのビルド工程がプリミティブを素通しするため
  VHACD を回避できる)。可撓物 (ホース・ケーブル) には collision を与えない
- メッシュは手続き的生成 (`tools/generate_meshes.py`) を基本とし、寸法定数を
  スクリプト冒頭に集約する。実機値への差し替えと再生成が 1 箇所で済む。
  README に載せる実測値も再現可能なスクリプトにする (例: weld-gun-x16005 の
  `tools/measure_throat.py`)

## レイアウト

```
<asset-slug>/
  urdf/<asset-slug>.urdf
  meshes/            # visual メッシュ (生成物)
  tools/             # メッシュ生成・計測スクリプト
  docs/              # 外観プレビュー・計測記録
  README.md          # アセットカード (参照実機・採った値・実測値・フレーム)
```

## Node.jsによる詳細モデルの著作

表示用形状とUSDを共有するモデルには [共通著作ライブラリ](./authoring/README.md) を使える。
製品固有の寸法・材質値・出典は製品側に残し、共通部は幾何生成と出力の処理に限定する。
visualをmmで作る場合の単位変換は一度だけ。frame/collision/inertialはSI単位で宣言する。
共通部の変更後は利用モデルで`npm ci`を再実行し、共通テスト・各モデルのテストと
再生成USDの比較を通す。各viewerのimport mapによる読み込みも確認する。

## rev 運用

**公開済み rev の中身は動かさないこと。** カタログのビルド工程は公開 rev の
ソースをコミット SHA でピン留めして取得している。アセットを変えたら**新しい rev を切る**
— 同一 rev で差し替えると、その rev を記録した既存 project が再現できなくなる。

ディレクトリを改名した場合も、旧 rev はピン留めした旧 SHA の旧パスを
参照し続けるので変更しない。カタログの製品 ID は `from_catalog()` の互換のため
改名しない (表示名とアセットのディレクトリ名で参照実機を示す)。
