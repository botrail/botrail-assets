# weld-gun-x1 — サーボスポットガン (X ガン / シザー式)

**状態: draft。** 構造・キネマティクス・フレーム・干渉形状は確定。寸法と質量は
実機データシート照合前のプレースホルダで、botrail 側の著作確定版で更新する
(weld-line-requests.md #2)。

![外観](docs/gun-iso.webp)

## 構成

X 形 (シザー式) — 固定アームと可動アームがピボットで交差する。

- **1 DOF** (`electrode_joint`, revolute, 軸 `0 -1 0`): 可動電極アーム。
  **正方向 = 開**。`q=0` が閉、`q=0.42 rad` (24.1°) が全開
- **リンク**: `mount` (root = フランジ接触面) → `body` → `electrode_arm`。
  `tcp` = 固定電極先端 (body 側の固定子リンク)
- **body**: 取付フランジ板 + ブラケット溶接構造、MFDC トランス (冷却フィン・
  端子箱)、サーボ + ボールねじ駆動、ピボットクレビス、固定アーム + 固定電極
  (上向き)、冷却水マニホールド / ホース / キックレスケーブル
- **electrode_arm**: ピボットブレード + ボールねじ連結テール、可動アーム +
  可動電極 (下向き)、冷却ホース

## 実測値 (メッシュから計測)

| 項目 | 値 |
|------|-----|
| のど深さ (電極軸 → ピボットクレビス前面) | 220 mm |
| 電極先端ストローク (q=0 → 0.42 rad) | 103 mm |
| 電極間クリアランス (q=0) | 5 mm |
| 電極間開口 (全開) | 108 mm |
| 質量 | 85 kg (body 72 + electrode_arm 13) |
| 電極加圧力 (設計宣言値) | 4500 N |

のど開口はアームのテーパーに沿って奥へ細くなる (y=0 断面の実測):

| 電極軸からの奥行き | 閉 (q=0) | 開 (q=0.42) |
|---|---|---|
| 35 mm | 102 mm | 165 mm |
| 75 mm | 89 mm | 194 mm |
| 115 mm | 75 mm | 158 mm |
| 155 mm | 59 mm | 119 mm |
| 195 mm | 41 mm | 78 mm |

## visual / collision の分離

- **visual** = `meshes/{body,electrode_arm}.stl`。`tools/generate_meshes.py` が
  生成する手続き的著作物 (trimesh のみに依存、ブーリアン不使用)。寸法定数は
  スクリプト冒頭の GEOMETRY ブロックに集約してあるので、確定版はそこを差し替えて
  再生成する
- **collision** = URDF のプリミティブ (box / cylinder) のみ、計 10 個。
  **のどの開口を保つのが目的**で、builder がプリミティブを素通しするため
  VHACD で開口が塞がれない。可撓物 (ホース・ケーブル) と機体内部のテールには
  collision を与えていない

検証済み (1.5 mm 薄板をのど中心面に差し込んだときの干渉):

| 差込み深さ | 閉 (q=0) | 開 (q=0.42) |
|---|---|---|
| 30 / 80 / 130 mm | 干渉なし | 干渉なし |
| 180 / 220 mm | 干渉 (アーム) | 干渉なし |
| 260 mm | 干渉 | 干渉 (のど奥) |

body ↔ electrode_arm の自己干渉は全可動域 (q=0 / 0.21 / 0.42) で無し。

## メッシュ再生成

```sh
python weld-gun-x1/tools/generate_meshes.py
```
